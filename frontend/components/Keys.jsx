import React from "react";

const Keys = ({ letter }) => {
  return (
    <div className="flex items-center justify-center p-3 bg-gray-800/30 border rounded-lg">
      {letter}
    </div>
  );
};

export default Keys;
